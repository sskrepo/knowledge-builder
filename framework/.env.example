# Picks framework/config/{KBF_ENV}.yaml at process start
KBF_ENV=dev

# OCI SDK auth — choose one:
OCI_AUTH_METHOD=instance_principal   # for OCI Compute / Functions
# OCI_AUTH_METHOD=config_file        # for local dev (uses ~/.oci/config)
# OCI_CONFIG_PROFILE=DEFAULT

LOG_LEVEL=INFO
