workflow_skill: weekly_26ai_executive_review_ppt_using_faaas_proje
persona: tpm
status: draft
trigger:
  on_request:
    enabled: true
    inputs:
    - name: input
      type: string
      description: Query or filter input
    output_format: pptx
    response_mode: artifact_url
skill_card:
  summary: weekly 26ai executive review PPT using FAaaS project sources
  use_when: 'User asks for: weekly 26ai executive review PPT using FAaaS project sources'
  example_invocations:
  - weekly 26ai executive review PPT using FAaaS project sources
  do_not_use_for: Single-fact lookups (use vector_search). Live operational data (use
    query_fleet).
requires_extractions:
- kb: tpm.weekly_26ai_executive_review_ppt_using_faaas_proje
  required_fields:
  - title
  - rag_status
  - milestones
  - workstreams
  - risks
  - next_steps
- kb: pm.pm_briefs
  required_fields:
  - summary
- kb: tpm.tpm_weekly_ops
  required_fields:
  - week_id
  - blockers
  - exec_asks
- kb: ops_mgr.ops_mgr_escalation
  required_fields:
  - scope
- kb: pm.pm_market_research
  required_fields:
  - sources
synthesis:
  output_format: pptx
  template: synthesis/templates/weekly_26ai_executive_review_ppt_using_faaas_proje.pptx
  slide_mapping: synthesis/mappings/weekly_26ai_executive_review_ppt_using_faaas_proje.yaml
  field_mapping:
    title:
      section: Title
      source_field: title
    summary:
      section: Summary
      source_field: summary
    week_id:
      section: Week Id
      source_field: week_id
    rag_status:
      section: Rag Status
      source_field: rag_status
    scope:
      section: Scope
      source_field: scope
    milestones:
      section: Milestones
      source_field: milestones
    workstreams:
      section: Workstreams
      source_field: workstreams
    blockers:
      section: Blockers
      source_field: blockers
    risks:
      section: Risks
      source_field: risks
    next_steps:
      section: Next Steps
      source_field: next_steps
    exec_asks:
      section: Exec Asks
      source_field: exec_asks
    sources:
      section: Sources
      source_field: sources
delivery:
  kind: filesystem
  path: ~/.kbf/outputs/weekly_26ai_executive_review_ppt_using_faaas_proje.pptx
eval:
  gold_set: eval/gold_sets/tpm-weekly_26ai_executive_review_ppt_using_faaas_proje-workflow.jsonl
  exit_criteria:
    field_accuracy: 0.85
    delivery_success_rate: 0.99
