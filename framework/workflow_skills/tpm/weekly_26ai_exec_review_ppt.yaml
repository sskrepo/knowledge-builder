workflow_skill: weekly_26ai_exec_review_ppt
persona: tpm
status: draft
trigger:
  on_request:
    enabled: true
    inputs:
    - name: input
      type: string
      description: Query or filter input
    output_format: pptx
    response_mode: artifact_url
skill_card:
  summary: weekly 26ai exec review ppt
  use_when: 'User asks for: weekly 26ai exec review ppt'
  example_invocations:
  - weekly 26ai exec review ppt
  do_not_use_for: Single-fact lookups (use vector_search). Live operational data (use
    query_fleet).
requires_extractions:
- kb: tpm.weekly_26ai_exec_review_ppt_data
  required_fields:
  - project
  - rag_status
  - risks
  - next_steps
- kb: tpm.tpm_weekly_ops
  required_fields:
  - exec_asks
- kb: pm.pm_market_research
  required_fields:
  - sources
synthesis:
  output_format: pptx
  template: synthesis/templates/weekly_26ai_exec_review_ppt.pptx
  slide_mapping: synthesis/mappings/weekly_26ai_exec_review_ppt.yaml
  field_mapping:
    project:
      section: Project
      source_field: project
    rag_status:
      section: Rag Status
      source_field: rag_status
    risks:
      section: Risks
      source_field: risks
    next_steps:
      section: Next Steps
      source_field: next_steps
    exec_asks:
      section: Exec Asks
      source_field: exec_asks
    sources:
      section: Sources
      source_field: sources
delivery:
  kind: filesystem
  path: ~/.kbf/outputs/weekly_26ai_exec_review_ppt.pptx
eval:
  gold_set: eval/gold_sets/tpm-weekly_26ai_exec_review_ppt-workflow.jsonl
  exit_criteria:
    field_accuracy: 0.85
    delivery_success_rate: 0.99
